import styled from 'styled-components'

export const InfosWrapper = styled.div`
  height: calc(100vh - 81px - 600px);

  .title {
    position: absolute;
    top: 744px;
    left: 32px;
    font-size: 32px;
    font-weight: 800;
    line-height: 1.125em;
    color: rgb(72, 72, 72);
  }
`
